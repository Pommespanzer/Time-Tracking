define([ 'backbone' ], function(Backbone) {
    return Backbone.Router.extend({
        routes: {
            'default': 'default'
        },

        initialize: function() {},

        'default': function() {}
    });
});